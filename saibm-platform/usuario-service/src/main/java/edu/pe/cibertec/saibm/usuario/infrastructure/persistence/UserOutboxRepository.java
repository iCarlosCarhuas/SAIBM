package edu.pe.cibertec.saibm.usuario.infrastructure.persistence; import java.util.*; import java.util.UUID; import org.springframework.data.domain.Pageable; import org.springframework.data.jpa.repository.*; public interface UserOutboxRepository extends JpaRepository<UserOutboxJpaEntity,UUID>{@Query("select e from UserOutboxJpaEntity e where e.published=false order by e.occurredAt")List<UserOutboxJpaEntity> pending(Pageable page);}
