package edu.pe.cibertec.saibm.usuario.infrastructure.migration; import java.nio.charset.StandardCharsets; import java.time.Instant; import java.util.UUID; import edu.pe.cibertec.saibm.usuario.domain.model.UserProfile; public final class UserBackfillMapper {private UserBackfillMapper(){}public static UserProfile map(LegacyUserRow row){return UserProfile.create(UUID.nameUUIDFromBytes(("legacy-user:"+row.id()).getBytes(StandardCharsets.UTF_8)),row.firstName(),row.lastName(),row.dni(),row.email(),Long.toString(row.id()),Instant.EPOCH);}}
