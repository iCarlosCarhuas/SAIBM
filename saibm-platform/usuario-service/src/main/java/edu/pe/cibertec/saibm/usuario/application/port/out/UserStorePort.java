package edu.pe.cibertec.saibm.usuario.application.port.out; import java.util.*; import edu.pe.cibertec.saibm.usuario.domain.model.UserProfile; public interface UserStorePort {Optional<UserProfile> find(UUID id);List<UserProfile> search(String query);void save(UserProfile profile);default Optional<UserProfile> byEmail(String email){return search(email).stream().filter(x->x.email().equalsIgnoreCase(email)).findFirst();}}
