package edu.pe.cibertec.saibm.inventario.infrastructure.persistence; import java.util.*; import org.springframework.data.jpa.repository.*; public interface OutboxJpaRepository extends JpaRepository<OutboxJpaEntity,UUID>{@Query("select e from OutboxJpaEntity e where e.published=false order by e.occurredAt")List<OutboxJpaEntity> pending(org.springframework.data.domain.Pageable page);}
